# Public Edge instance defaults. Public origins live in platform publications.json.

# Host address used to publish the platform-owned HTTP/HTTPS entrypoints.
# 0.0.0.0 is portable across cloud and local Linux hosts; override with a
# specific host address when the deployment requires interface scoping.
SERVER_EDGE_PUBLIC_BIND_IP=0.0.0.0
