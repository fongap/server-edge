# Infra runtime defaults. Instance overrides live in /opt/server-edge/config/infra.env.

# Overlay network
SERVER_EDGE_INFRA_OVERLAY=auto

# Tailscale implementation settings
SERVER_EDGE_INFRA_TAILSCALE_HOSTNAME=auto
SERVER_EDGE_INFRA_TAILSCALE_ACCEPT_DNS=false
